name: ares_builder
description: Ares Asistan Otonom Word to APK Oluşturucu
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  docx_to_text: ^1.0.0
  http: ^1.2.0
  shared_preferences: ^2.2.2
  speech_to_text: ^6.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
